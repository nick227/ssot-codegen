// @generated
// This file is automatically generated. Do not edit manually.

import type { 
  Swipe, 
  SwipeCreate, 
  SwipeUpdate, 
  SwipeQuery,
  ListResponse 
} from '../../types'
import type { SDK } from '../../index'
import { stableKey } from './shared/query-helpers'

/**
 * Swipe query factory - accepts API client for flexibility
 * 
 * These are pure query definitions that work with ANY SDK instance.
 * Inject your configured client via factory pattern.
 */
export function createSwipeQueries(api: SDK) {
  return {
    /**
     * Core queries - Standard CRUD operations
     */
    all: {
      /**
       * Get single Swipe by ID
       */
      get: (id: string) => ({
        queryKey: stableKey('swipe', id),
        queryFn: async (): Promise<Swipe | null> => {
          const result = await api.swipe.get(id)
          return result || null
        }
      }),
      
      /**
       * List Swipe records
       */
      list: (query?: SwipeQuery) => ({
        queryKey: stableKey('swipes', query),
        queryFn: async (): Promise<ListResponse<Swipe>> => {
          const result = await api.swipe.list(query)
          return result || { data: [], meta: { total: 0, skip: 0, take: 20, hasMore: false } }
        }
      }),
      
      /**
       * Find Swipe by any field
       */
      findOne: (where: Partial<Swipe>) => ({
        queryKey: ['swipe', 'query', JSON.stringify(where)],
        queryFn: async (): Promise<Swipe | null> => {
          const result = await api.swipe.findOne(where)
          return result || null
        }
      }),
      
      /**
       * Count Swipe records
       */
      count: (query?: Pick<SwipeQuery, 'where'>) => ({
        queryKey: stableKey('swipes:count', query),
        queryFn: async (): Promise<number> => {
          const result = await api.swipe.count(query as Partial<SwipeQuery>)
          return result || 0
        }
      })
    }
  }
}

/**
 * Swipe mutation factory - accepts API client
 */
export function createSwipeMutations(api: SDK) {
  return {
    /**
     * Create Swipe
     */
    create: () => ({
      mutationKey: ['swipe', 'create'] as const,
      mutationFn: async (data: SwipeCreate): Promise<Swipe> => 
        api.swipe.create(data)
    }),
    
    /**
     * Update Swipe
     */
    update: () => ({
      mutationKey: ['swipe', 'update'] as const,
      mutationFn: async ({ id, data }: { id: string; data: SwipeUpdate }): Promise<Swipe | null> => 
        api.swipe.update(id, data)
    }),
    
    /**
     * Delete Swipe
     */
    delete: () => ({
      mutationKey: ['swipe', 'delete'] as const,
      mutationFn: async (id: string): Promise<boolean> => 
        api.swipe.delete(id)
    })
  }
}

/**
 * Swipe infinite query factory - accepts API client
 */
export function createSwipeInfinite(api: SDK) {
  return {
    /**
     * Infinite scroll Swipe list
     */
    list: (query?: Omit<SwipeQuery, 'skip'>) => {
      const pageSize = query?.take || 20
      
      return {
        queryKey: stableKey('swipes:infinite', query),
        queryFn: async (context: { pageParam: number }) => {
          const page = context.pageParam || 0
          return api.swipe.list({
            ...query,
            skip: page * pageSize,
            take: pageSize
          })
        },
        getNextPageParam: (lastPage: ListResponse<Swipe>, allPages: any[]) =>
          lastPage.meta.hasMore ? allPages.length : undefined,
        initialPageParam: 0
      }
    }
  }
}
