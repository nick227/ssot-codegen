// @generated
// This file is automatically generated. Do not edit manually.

import { Router, type Router as RouterType } from 'express'
import * as messageController from '@/controllers/message/index.js'

export const messageRouter: RouterType = Router()

// List all Message records (simple pagination)
messageRouter.get('/', messageController.listMessages)

// Search Message records (complex filtering via POST body)
messageRouter.post('/search', messageController.searchMessages)

// Get Message by ID
messageRouter.get('/:id', messageController.getMessage)

// Create Message
messageRouter.post('/', messageController.createMessage)

// Update Message
messageRouter.put('/:id', messageController.updateMessage)
messageRouter.patch('/:id', messageController.updateMessage)

// Delete Message
messageRouter.delete('/:id', messageController.deleteMessage)

// Count Message records
messageRouter.get('/meta/count', messageController.countMessages)

// Bulk operations
messageRouter.post('/bulk/create', messageController.bulkCreateMessages)
messageRouter.put('/bulk/update', messageController.bulkUpdateMessages)
messageRouter.delete('/bulk/delete', messageController.bulkDeleteMessages)

