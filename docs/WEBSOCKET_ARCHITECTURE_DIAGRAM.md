# WebSocket Architecture - Visual Diagrams

**Purpose**: Visual representation of WebSocket integration  
**Companion to**: WEBSOCKET_STRATEGY.md  

---

## 📊 System Architecture Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                        UI LAYER (React)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  ListPage    │  │  DetailPage  │  │  FormPage    │         │
│  │  Component   │  │  Component   │  │  Component   │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            │                                    │
│                            ▼                                    │
│                   NO CHANGES TO UI!                             │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ uses same hooks
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                       HOOK LAYER (React)                        │
│  ┌────────────┐  ┌───────────┐  ┌────────────┐  ┌───────────┐ │
│  │  useList() │  │  useGet() │  │ useCreate()│  │useUpdate()│ │
│  └─────┬──────┘  └─────┬─────┘  └─────┬──────┘  └─────┬─────┘ │
│        │               │                │               │       │
│        │ ┌─────────────┴────────────────┴───────────────┘       │
│        │ │ + Added: subscription logic                          │
│        │ │ + Added: update handling                             │
│        │ │ ✓ Maintains: same interface                          │
│        └─┴──────────────────────────────────────────────────────┤
│                            │                                    │
│                            ▼                                    │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ calls DataClient interface
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   TRANSPORT LAYER (NEW)                         │
│                                                                 │
│                    ┌─────────────────┐                          │
│                    │ HybridDataClient│                          │
│                    │  (Smart Router) │                          │
│                    └────────┬────────┘                          │
│                             │                                   │
│              ┌──────────────┴──────────────┐                    │
│              │                             │                    │
│              ▼                             ▼                    │
│    ┌──────────────────┐         ┌──────────────────┐           │
│    │  HTTPTransport   │         │ WebSocketTransport│           │
│    │  (HTTP/fetch)    │         │   (WS + cache)   │           │
│    └────────┬─────────┘         └────────┬─────────┘           │
│             │                            │                      │
│             │ Queries & Mutations        │ Subscriptions        │
│             │ (reliable)                 │ (real-time)          │
│             │                            │                      │
└─────────────┼────────────────────────────┼──────────────────────┘
              │                            │
              │                            │
              ▼                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                       SERVER LAYER                              │
│                                                                 │
│    ┌──────────────────┐         ┌──────────────────┐           │
│    │   REST API       │         │ WebSocket Gateway│           │
│    │   (Express)      │         │   (ws library)   │           │
│    └────────┬─────────┘         └────────┬─────────┘           │
│             │                            │                      │
│             │                            │                      │
│             │   ┌────────────────────┐   │                      │
│             └──▶│   Prisma Client    │◀──┘                      │
│                 │   (Database)       │                          │
│                 └────────────────────┘                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Flow 1: Initial Page Load (HTTP)

```
┌────────┐                                              ┌────────┐
│   UI   │                                              │ Server │
└───┬────┘                                              └───┬────┘
    │                                                       │
    │ 1. useList() hook mounts                              │
    │                                                       │
    │ 2. calls client.list()                                │
    │────────────────────────────────────────────────────▶  │
    │                                                       │
    │                          3. SELECT * FROM messages    │
    │                                         (Prisma)      │
    │                                                       │
    │ ◀────────────────────────────────────────────────────┤
    │    4. HTTP 200 { items: [...], total: 100 }          │
    │                                                       │
    │ 5. setData(items)                                     │
    │    UI renders list                                    │
    │                                                       │
    │ 6. IF subscribe option enabled:                       │
    │    client.subscribe('Message:list', handleUpdate)     │
    │────────────────────────────────────────────────────▶  │
    │                          7. WS: { type: 'subscribe' } │
    │                                                       │
    │ ◀────────────────────────────────────────────────────┤
    │    8. WS: { type: 'subscribed', channel: '...' }     │
    │                                                       │
    ▼                                                       ▼
  Ready                                              Listening
```

---

### Flow 2: Real-Time Update (WebSocket)

```
┌──────────┐         ┌────────┐                    ┌────────┐
│ Client A │         │ Client B│                    │ Server │
└────┬─────┘         └───┬────┘                    └───┬────┘
     │                   │                              │
     │ 1. User creates message                          │
     │                   │                              │
     │ POST /api/messages                               │
     │──────────────────────────────────────────────────▶
     │                   │                              │
     │                   │  2. prisma.message.create()  │
     │                   │                              │
     │ ◀─────────────────────────────────────────────────
     │ HTTP 200 (new message)                           │
     │                   │                              │
     │                   │  3. wsGateway.broadcast()    │
     │                   │     'Message:created'        │
     │                   │                              │
     │ WS: update ◀──────┼──────────────────────────────┤
     │ (own client)      │                              │
     │                   │                              │
     │                   │ WS: update ◀─────────────────┤
     │                   │ (other client)               │
     │                   │                              │
     │ 4. handleUpdate() │  4. handleUpdate()           │
     │    setData(...)   │     setData(...)             │
     │    UI updates     │     UI updates               │
     │                   │                              │
     ▼                   ▼                              ▼
  Updated             Updated                       Broadcast
                                                     Complete
```

---

### Flow 3: Smart Transport Selection

```
┌────────────────────────────────────────────────────────────────┐
│                      HybridDataClient                          │
└────────────────────────────────────────────────────────────────┘
                             │
                             │ client.list() called
                             ▼
                   ┌─────────────────────┐
                   │ Is WS connected?    │
                   └──────┬──────────────┘
                          │
                ┌─────────┴─────────┐
                │ YES               │ NO
                ▼                   ▼
    ┌───────────────────┐   ┌────────────────┐
    │ Has subscription? │   │  Use HTTP      │
    └────────┬──────────┘   │  (default)     │
             │              └────────────────┘
      ┌──────┴──────┐
      │ YES         │ NO
      ▼             ▼
┌──────────┐  ┌──────────┐
│ Use WS   │  │ Use HTTP │
│ (cache)  │  │ (fetch)  │
└──────────┘  └──────────┘
```

---

## 🏛️ Class Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     <<interface>>                           │
│                     DataClient<T>                           │
├─────────────────────────────────────────────────────────────┤
│ + list(params?): Promise<ListResponse<T>>                   │
│ + get(id): Promise<T | null>                                │
│ + create(data): Promise<T>                                  │
│ + update(id, data): Promise<T>                              │
│ + delete(id): Promise<boolean>                              │
│ + subscribe?(channel, callback): Unsubscribe                │
│ + onUpdate?(id, callback): Unsubscribe                      │
│ + onDelete?(id, callback): Unsubscribe                      │
└─────────────────────────────────────────────────────────────┘
                          ▲
                          │ implements
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        │                 │                 │
┌───────┴────────┐ ┌──────┴──────┐ ┌───────┴─────────┐
│ HTTPTransport  │ │WebSocketTrans│ │HybridDataClient │
│                │ │   port       │ │                 │
├────────────────┤ ├──────────────┤ ├─────────────────┤
│- client        │ │- ws          │ │- http           │
│- basePath      │ │- cache       │ │- ws?            │
├────────────────┤ │- subs        │ ├─────────────────┤
│+ list()        │ ├──────────────┤ │+ list()         │
│+ get()         │ │+ list()      │ │  → smart route  │
│+ create()      │ │+ get()       │ │+ create()       │
│+ update()      │ │+ subscribe() │ │  → always HTTP  │
│+ delete()      │ │+ onUpdate()  │ │+ subscribe()    │
│                │ │+ onDelete()  │ │  → delegate WS  │
└────────────────┘ └──────────────┘ └─────────────────┘
        │                 │
        ▼                 ▼
┌────────────────┐ ┌──────────────┐
│ BaseAPIClient  │ │  WebSocket   │
│  (existing)    │ │  (native)    │
└────────────────┘ └──────────────┘
```

---

## 📡 WebSocket Message Protocol

### Client → Server

```typescript
// Subscribe to channel
{
  type: 'subscribe',
  channel: 'Message:list'
}

// Unsubscribe from channel
{
  type: 'unsubscribe',
  channel: 'Message:list'
}

// Request data
{
  type: 'request',
  requestId: 'req-123',
  model: 'Message',
  action: 'list',
  params: { where: { published: true } }
}
```

---

### Server → Client

```typescript
// Subscription confirmed
{
  type: 'subscribed',
  channel: 'Message:list'
}

// Data update broadcast
{
  type: 'update',
  channel: 'Message:list',
  data: {
    type: 'created',
    data: { id: 1, text: 'Hello' },
    timestamp: 1699999999999
  }
}

// Response to request
{
  type: 'response',
  requestId: 'req-123',
  data: { items: [...], total: 100 }
}

// Error
{
  type: 'error',
  requestId: 'req-123',
  error: 'Unauthorized'
}
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                        │
└─────────────────────────────────────────────────────────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
    ┌─────────────┐  ┌──────────────┐  ┌──────────────┐
    │ 1. AUTH     │  │ 2. AUTHZ     │  │ 3. RATE      │
    │ (Connection)│  │ (Channel)    │  │    LIMIT     │
    └──────┬──────┘  └──────┬───────┘  └──────┬───────┘
           │                │                  │
           ▼                ▼                  ▼
    ┌──────────────────────────────────────────────────┐
    │ Token validation    Channel ACL    Max subs/user │
    │ JWT/Session        Per-model perms Max conn/user │
    │ User extraction    Field filtering               │
    └──────────────────────────────────────────────────┘
```

---

## 🎯 Decision Tree Flowchart

```
                    ┌─────────────────────┐
                    │  New SSOT Project   │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ Schema has Message, │
                    │ Notification, Chat? │
                    └──────┬──────────────┘
                           │
                    ┌──────┴──────┐
                    │ YES         │ NO
                    ▼             ▼
         ┌────────────────┐  ┌─────────────────┐
         │ Auto-enable WS │  │ Prompt user:    │
         │ Generate WS    │  │ Enable WS?      │
         │ code           │  └────┬────────────┘
         └────────────────┘       │
                    │       ┌─────┴─────┐
                    │       │ YES       │ NO
                    ▼       ▼           ▼
         ┌────────────────────────┐  ┌────────────────┐
         │ Generate:              │  │ Generate:      │
         │ - HTTP + WS transports │  │ - HTTP only    │
         │ - Hybrid client        │  │ - BaseClient   │
         │ - WS gateway           │  │ (existing)     │
         │ - Updated hooks        │  └────────────────┘
         └────────────────────────┘
```

---

## 📦 File Generation Matrix

```
┌────────────────────────────────────────────────────────────┐
│                   Generated Files                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  HTTP Only (Default)    │  HTTP + WebSocket (Optional)    │
│ ────────────────────────┼─────────────────────────────────│
│                         │                                 │
│  generated/             │  generated/                     │
│  ├─ src/                │  ├─ src/                        │
│  │  ├─ api/             │  │  ├─ api/                     │
│  │  │  └─ routes/       │  │  │  └─ routes/               │
│  │  │     ├─ user.ts    │  │  │     ├─ user.ts            │
│  │  │     └─ post.ts    │  │  │     └─ message.ts ⭐      │
│  │  │                   │  │  │        (+ broadcast calls) │
│  │  ├─ sdk/             │  │  │                           │
│  │  │  ├─ client/       │  │  ├─ websockets/ ⭐           │
│  │  │  │  └─ http.ts    │  │  │  ├─ gateway.ts            │
│  │  │  └─ hooks/        │  │  │  ├─ channels.ts           │
│  │  │     └─ useList.ts │  │  │  └─ auth.ts               │
│  │  │                   │  │  │                           │
│  │  └─ prisma/          │  │  ├─ sdk/                     │
│  │     └─ schema.prisma │  │  │  ├─ client/               │
│  │                      │  │  │  │  ├─ http.ts             │
│  │                      │  │  │  │  ├─ ws.ts ⭐            │
│  │                      │  │  │  │  └─ hybrid.ts ⭐        │
│  │                      │  │  │  └─ hooks/                │
│  │                      │  │  │     └─ useList.ts ⭐       │
│  │                      │  │  │        (+ subscriptions)  │
│  │                      │  │  │                           │
│  │                      │  │  └─ prisma/                  │
│  │                      │  │     └─ schema.prisma         │
│  │                      │  │                              │
│  Lines: ~2,000          │  Lines: ~3,500                  │
│  Files: 15              │  Files: 22                      │
│                         │                                 │
└────────────────────────────────────────────────────────────┘

⭐ = New or significantly updated for WebSocket support
```

---

## 🔄 Hook Lifecycle with WebSocket

```
┌─────────────────────────────────────────────────────────────┐
│              useList() Hook Lifecycle                       │
└─────────────────────────────────────────────────────────────┘

1. Component Mount
   ├─ useList(client, params, { subscribe: true })
   │
   ├─ useEffect(() => {
   │    ├─ 2. Initial Fetch (HTTP)
   │    │  client.list(params)
   │    │    └─> HTTP: GET /api/messages?page=1
   │    │
   │    ├─ 3. Set Initial Data
   │    │  setData(result.items)
   │    │  setLoading(false)
   │    │
   │    └─ 4. Subscribe (WebSocket)
   │       const unsub = client.subscribe('Message:list', update => {
   │         ├─ 5. Receive Update
   │         │  { type: 'created', data: {...} }
   │         │
   │         ├─ 6. Apply Update
   │         │  setData(prev => [...prev, update.data])
   │         │
   │         └─ 7. UI Re-renders
   │            (React automatically)
   │       })
   │
   │    return () => {
   │      ├─ 8. Component Unmount
   │      │  unsub()
   │      │    └─> WS: { type: 'unsubscribe' }
   │      │
   │      └─ 9. Cleanup
   │         Clear cache, close connection if last subscriber
   │    }
   │  }, [client, params])
   │
   └─ return { data, isLoading, error, refetch }

Timeline:
─────────────────────────────────────────────────────────────
│ Mount │ Fetch │ Render │      Subscribe      │ Unmount   │
│       │       │        │                     │           │
│   0ms │  50ms │  51ms  │  100ms -> infinity  │  cleanup  │
─────────────────────────────────────────────────────────────
        ▲HTTP▲           ▲────────WS──────────▶
```

---

## 🏗️ Implementation Phases Visual

```
┌────────────────────────────────────────────────────────────┐
│                    5-Day Plan                              │
└────────────────────────────────────────────────────────────┘

Day 1: Transport Abstraction
┌─────────────────────────────────────┐
│ DataClient interface                │
│ HTTPTransport wrapper               │ ← 200 lines
│ WebSocketTransport implementation   │ ← 300 lines
│ HybridDataClient router             │ ← 150 lines
└─────────────────────────────────────┘
        ↓ Test: Unit tests

Day 2: Hook Integration
┌─────────────────────────────────────┐
│ Update useList with subscriptions   │ ← 50 lines
│ Update useGet with item tracking    │ ← 40 lines
│ Update useCreate (no changes)       │ ← 0 lines
│ Create useDataClient context        │ ← 80 lines
└─────────────────────────────────────┘
        ↓ Test: Hook tests with mocks

Day 3: Server Gateway
┌─────────────────────────────────────┐
│ WebSocket gateway generator         │ ← 400 lines
│ Channel routing & permissions       │ ← 200 lines
│ Update route generators             │ ← 100 lines
│ Authentication middleware           │ ← 80 lines
└─────────────────────────────────────┘
        ↓ Test: Integration tests

Day 4: Configuration
┌─────────────────────────────────────┐
│ ssot.config.ts schema               │ ← 100 lines
│ Auto-detection logic                │ ← 80 lines
│ Prompt updates                      │ ← 50 lines
│ Conditional generation              │ ← 150 lines
└─────────────────────────────────────┘
        ↓ Test: E2E generation test

Day 5: Documentation
┌─────────────────────────────────────┐
│ User guide (DONE ✓)                 │
│ Example projects                    │ ← 2 examples
│ Migration guide                     │ ← 1 guide
│ README updates                      │ ← Updates
└─────────────────────────────────────┘
        ↓ Test: Doc examples work

Total Lines: ~2,000 new code
Total Files: ~15 new files
Test Coverage: > 90%
```

---

## 📈 Performance Comparison

```
HTTP Only (Polling)
───────────────────────────────────────────────────────────
Request → Wait → Response → Wait → Request → ...
          5s              5s
          
Total: ~10 requests/min
Latency: 0-5s (average 2.5s)
Overhead: ~100KB/min (headers + data)


HTTP + WebSocket
───────────────────────────────────────────────────────────
Initial Request → Subscribe → [Updates pushed instantly]
                               ↓      ↓      ↓
                              50ms   50ms   50ms
                              
Total: 1 request + N updates
Latency: ~50ms (network only)
Overhead: ~10KB/min (just data, no headers)

Improvement: 50x less latency, 10x less bandwidth
```

---

## 🎯 Summary

This architecture provides:

✅ **Clean Abstraction**: DataClient interface  
✅ **Smart Routing**: Hybrid client chooses best transport  
✅ **Zero UI Changes**: Hooks handle everything  
✅ **Optional**: Generate WS code only when needed  
✅ **Secure**: Multi-layer security  
✅ **Performant**: Caching, batching, reconnection  
✅ **Testable**: Unit → Integration → E2E  
✅ **Maintainable**: Short files, clear responsibilities  

**Ready to implement!** 🚀

---

**See Also**:
- [WEBSOCKET_STRATEGY.md](./WEBSOCKET_STRATEGY.md) - Implementation details
- [WEBSOCKET_CONFIGURATION_GUIDE.md](./WEBSOCKET_CONFIGURATION_GUIDE.md) - User guide
- [WEBSOCKET_IMPLEMENTATION_EXAMPLES.md](./WEBSOCKET_IMPLEMENTATION_EXAMPLES.md) - Code examples

