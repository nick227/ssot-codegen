{
  "name": "@ssot-codegen/cli",
  "version": "0.5.0",
  "type": "module",
  "description": "CLI tool for SSOT Codegen",
  "bin": {
    "ssot": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@ssot-codegen/gen": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0"
  }
}

