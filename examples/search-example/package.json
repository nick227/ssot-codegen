{
  "name": "search-example",
  "version": "1.0.0",
  "description": "Full-text search example with SSOT Codegen",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "seed": "tsx seed.ts",
    "dev": "cd generated && npm run dev",
    "start": "cd generated && npm start"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@ssot-codegen/gen": "workspace:*",
    "prisma": "^5.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}

